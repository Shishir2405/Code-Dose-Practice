/**
 * Question 17
 * Write an arrow function that takes two parameters and returns an object that includes these two parameters as properties.
 * The properties should be named a and b.
 */
const createObject = (a, b) => ({ a, b });

// Example usage:
console.log(createObject(1, 2));
// Output: { a: 1, b: 2 }
